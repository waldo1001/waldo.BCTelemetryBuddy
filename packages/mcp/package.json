{
    "name": "bc-telemetry-buddy-mcp",
    "version": "2.3.0",
    "description": "Model Context Protocol server for Business Central telemetry",
    "type": "commonjs",
    "main": "./dist/server.js",
    "types": "./dist/server.d.ts",
    "bin": {
        "bctb-mcp": "./dist/cli.js"
    },
    "scripts": {
        "build": "npm run build:shared && npm run build:server && npm run build:cli && npm run copy:launcher",
        "build:shared": "cd ../shared && npm run build",
        "build:server": "node scripts/generate-version.js && esbuild src/server.ts --bundle --outfile=dist/server.js --platform=node --format=cjs --sourcemap --minify",
        "build:cli": "node scripts/generate-version.js && esbuild src/cli.ts --bundle --outfile=dist/cli.js --platform=node --format=cjs --sourcemap --minify --banner:js=\"#!/usr/bin/env node\"",
        "copy:launcher": "node -e \"require('fs').copyFileSync('launcher.js', 'dist/launcher.js')\"",
        "dev": "tsc --build --watch",
        "compile": "tsc --noEmit",
        "start": "node dist/server.js",
        "pretest": "node scripts/generate-version.js",
        "test": "jest --forceExit",
        "test:coverage": "jest --coverage --forceExit",
        "test:watch": "jest --watch",
        "clean": "rimraf dist"
    },
    "dependencies": {
        "applicationinsights": "^3.12.0",
        "commander": "^12.0.0",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "@bctb/shared": "file:../shared",
        "@types/express": "^4.17.21",
        "@types/jest": "^29.5.0",
        "@types/node": "^22.18.11",
        "esbuild": "^0.27.0",
        "jest": "^29.7.0",
        "rimraf": "^6.0.1",
        "ts-jest": "^29.4.5",
        "typescript": "^5.3.0"
    }
}